---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:
  - name: "Hostname"
    path: "30-hosts/%{facts.hostname}.yaml"
  - name: "Domain"
    path: "20-domains/%{facts.domain}.yaml"
  - name: "OS Version"
    path: "10-os/%{facts.os.name}/%{facts.os.release.major}.yaml"
  - name: "Operating System"
    path: "10-os/%{facts.os.name}.yaml"
  - name: "Global"
    path: "0-global.yaml"
